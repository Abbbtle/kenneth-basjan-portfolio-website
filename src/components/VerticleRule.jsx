import React from 'react'

const VerticleRule = () => {
    const verticalRuleStyle = {
        borderLeft: '2px solid #fff', // Adjust color and size as needed
        height: '100px', // Adjust height as needed
        margin: '0 auto', // Center the line
      };
    
      return (
        <div style={verticalRuleStyle}></div>
      );
    };

export default VerticleRule